import { LimitWordsPipePipe } from './limit-words-pipe.pipe';

describe('LimitWordsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new LimitWordsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
